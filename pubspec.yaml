name: runner_bridge_game
description: A simple offline Flutter runner game with bridges, ropes, sea, coins, and a monster chase.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
